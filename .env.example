# ==================== DJANGO BACKEND ====================
# Tắt debug mode trên production
DEBUG=False

# Tạo SECRET_KEY mới cho production: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-new-secret-key-here

# Domain VPS của bạn (ví dụ: example.com, 192.168.1.100)
ALLOWED_HOSTS=your-vps-domain.com,www.your-vps-domain.com,your-vps-ip

# ==================== DATABASE ====================
# Nếu dùng SQLite (như hiện tại)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=/app/db.sqlite3

# HOẶC nếu muốn dùng PostgreSQL (production khuyến nghị)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=robot_db
# DATABASE_USER=postgres_user
# DATABASE_PASSWORD=secure_password
# DATABASE_HOST=db
# DATABASE_PORT=5432

# ==================== MONGODB ====================
# URL MongoDB (đã có trên MongoDB Atlas)
MONGO_URI=mongodb+srv://hoangbao27092004_db_user:baozzz123@cluster0.q7fvyyw.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Tên database và collection MongoDB
MONGO_DB_NAME=robot_users
MONGO_USERS_COLLECTION=users

# ==================== ROBOT CONTROL ====================
# Secret key cho đăng ký robot
ROBOT_REG_SECRET=your-secure-robot-secret-key

# ==================== OPTIONAL (PRODUCTION) ====================
# Email backend cho password reset
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
